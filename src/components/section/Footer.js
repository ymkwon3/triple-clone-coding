import { Flex } from "../../elements";

const Footer = () => {
    const style = {
        backgroundColor: "#FAFAFA"
    }
    return (
        <Flex styles={style}>
            푸터입니다.
        </Flex>
    )
}

export default Footer;